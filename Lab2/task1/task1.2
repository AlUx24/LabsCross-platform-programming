public class IntervalChecker {
    public static void main(String[] args) {
        int inputNumber = 15; 

        boolean isInInterval = (inputNumber > -15 && inputNumber <= 12) ||
                               (inputNumber > 14 && inputNumber < 17) ||
                               (inputNumber >= 19);

        System.out.println(**isInInterval**);
    }
}
